name: CI
on:
  push:
    branches: [main, phase-1-foundation, phase-2-advanced-features, phase-3-production-hardening, phase-3-hardening-increment]
  pull_request:
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - run: npm run lint
      - run: npm run typecheck
      - run: npm test
      - run: npm run build
